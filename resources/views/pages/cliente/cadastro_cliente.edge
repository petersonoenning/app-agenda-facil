<!DOCTYPE html>
<html lang="PT-BR">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>Cadastro de Cliente</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="/resources/vendors/mdi/css/materialdesignicons.min.css">
  <link rel="stylesheet" href="/resources/vendors/css/vendor.bundle.base.css">
  <link rel="stylesheet" href="/resources/css/app.css">
  <link rel="shortcut icon" href="/resources/images/favicon.png" />
</head>

<body>
  @include('partials/_navbar')
  <div class="container-fluid">
    <div class="row">
      @include('partials/_sidebar')
      <main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-4">
        <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
          <h1 class="h2">Cadastro de Cliente</h1>
        </div>

        @flashMessage('notificacao')
        <div class="alert alert-{{ $message.type }}">
          {{ $message.message }}
        </div>
        @end

        <div class="card">
          <div class="card-body">
            <form class="forms-sample" method="POST"
              action="{{ !cliente ? route('clientes.store') : route('clientes.update', { id: cliente.id }, { qs: { _method: 'PUT' } }) }}">
              {{ csrfField() }}

              <div class="form-group">
                <label for="inputNome">Nome</label>
                <input type="text" class="form-control" id="inputNome" value="{{ old('nome') || cliente?.nome ||'' }}"
                  name="nome" placeholder="Informe o nome" required>
                @inputError('nome')
                <span class="text-danger"> {{ $messages.join(', ') }} </span>
                @end
              </div>

              <div class="form-group">
                <label for="inputTelefone">Telefone</label>
                <input type="text" class="form-control" id="inputTelefone"
                  value="{{ old('telefone') || cliente?.telefone ||'' }}" name="telefone"
                  placeholder="Informe o telefone" required>
                @inputError('telefone')
                <span class="text-danger"> {{ $messages.join(', ') }} </span>
                @end
              </div>

              <div class="form-group">
                <label for="inputEmail">Email</label>
                <input type="email" class="form-control" id="inputEmail" name="email"
                  value="{{ old('email') || cliente?.email || '' }}" placeholder="Informe o email" required>
                @inputError('email')
                <span class="text-danger"> {{ $messages.join(', ') }} </span>
                @end
              </div>

              <div class="form-group">
                <label for="inputIdade">Idade</label>
                <input type="number" class="form-control" id="inputIdade" name="idade"
                  value="{{ old('idade') || cliente?.idade || '' }}" placeholder="Informe a idade" required>
                @inputError('idade')
                <span class="text-danger"> {{ $messages.join(', ') }} </span>
                @end
              </div>

              <button type="submit" class="btn btn-primary mr-2">Salvar</button>
              <a href="{{ route('clientes.index') }}" class="btn btn-dark">Cancelar</a>
            </form>
          </div>
        </div>
      </main>
    </div>
  </div>

  <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
  <script>
    $(document).ready(function () {
      $('#cliente-form').on('submit', function (e) {
        e.preventDefault();
        const data = {
          nome: $('#inputNome').val(),
          telefone: $('#inputTelefone').val(),
          email: $('#inputEmail').val(),
          idade: $('#inputIdade').val(),
        };

        $.ajax({
          url: '/api/clientes',
          type: 'POST',
          data: data,
          success: function (response) {
            alert('Cliente cadastrado com sucesso!');
            $('#cliente-form')[0].reset();
          },
          error: function (xhr) {
            alert('Erro ao cadastrar cliente: ' + xhr.responseText);
          }
        });
      });
    });
  </script>
</body>

</html>
