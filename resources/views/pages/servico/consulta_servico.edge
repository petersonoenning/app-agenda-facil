<!DOCTYPE html>
<html lang="PT-BR">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>Consulta de Serviço</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="/resources/vendors/mdi/css/materialdesignicons.min.css">
  <link rel="stylesheet" href="/resources/vendors/css/vendor.bundle.base.css">
  <link rel="stylesheet" href="/resources/css/app.css">
  <link rel="shortcut icon" href="/resources/images/favicon.png" />
</head>

<body>
  @include('partials/_navbar')
  <div class="container-fluid">
    <div class="row">
      @include('partials/_sidebar')
      <main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-4">
        <div class="page-header">
          <h3 class="page-title">Serviços</h3>
        </div>

        @flashMessage('notificacao')
        <div class="alert alert-{{ $message.type }}">
          {{ $message.message }}
        </div>
        @end

        <div class="row">
          <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
              <div class="card-body">
                @if(!servicos || servicos.length === 0)
                <div class="alert alert-secondary">
                  <i class="mdi mdi-alert-box icon-md"></i> Nenhum serviço encontrado na base de dados!
                </div>
                @else
                <div class="table-responsive">
                  <table class="table">
                    <thead>
                      <tr>
                        <th>#</th>
                        <th>Nome</th>
                        <th>Descrição</th>
                        <th>Valor</th>
                        <th>Opções</th>
                      </tr>
                    </thead>
                    <tbody>
                      @each((servico, index) in servicos)
                      <tr>
                        <td>{{ index + 1 }}</td>
                        <td>{{ servico.nome }}</td>
                        <td>{{ servico.descricao }}</td>
                        <td>R$ {{ number_format(servico.valor, 2, ',', '.') }}</td>
                        <td style="display: flex;">
                          <a href="{{ route('servicos.edit', [servico.id]) }}" class="btn btn-warning btn-sm mr-1">
                            Editar
                          </a>

                          <form id="form-delete-{{ servico.id }}" method="POST"
                            action="{{ route('servicos.destroy', { id: servico.id }, { qs: { _method: 'DELETE' } }) }}">
                            {{ csrfField() }}

                            <a href="#" onclick="event.preventDefault(); document.getElementById('form-delete-{{ servico.id }}').submit();">
                              <i class="mdi mdi-delete-variant"></i>
                            </a>
                          </form>
                        </td>
                      </tr>
                      @end
                    </tbody>
                  </table>
                </div>
                @endif
              </div>
            </div>
          </div>
        </div>
      </main>
    </div>
  </div>

  <script src="/resources/vendors/js/vendor.bundle.base.js"></script>
  <script src="/resources/js/off-canvas.js"></script>
  <script src="/resources/js/hoverable-collapse.js"></script>
  <script src="/resources/js/misc.js"></script>
  <script src="/resources/js/settings.js"></script>
  <script src="/resources/js/todolist.js"></script>
</body>

</html>
