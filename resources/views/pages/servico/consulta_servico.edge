<!DOCTYPE html>
<html lang="PT-BR">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>Consulta de Serviço</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="/resources/vendors/mdi/css/materialdesignicons.min.css">
  <link rel="stylesheet" href="/resources/vendors/css/vendor.bundle.base.css">
  <link rel="stylesheet" href="/resources/css/app.css">
  <link rel="shortcut icon" href="/resources/images/favicon.png" />
</head>

<body>
  @include('partials/_navbar')
  <div class="container-fluid">
    <div class="row">
      @include('partials/_sidebar')
      <main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-4">
        <div class="page-header">
          <h3 class="page-title">Serviços</h3>
          <a class="btn btn-primary btn-fw" href="{{ route('servicos.create') }}">+ Criar Serviço</a>
        </div>

        @flashMessage('notificacao')
        <div class="alert alert-{{ $message.type }}">
          {{ $message.message }}
        </div>
        @end

        <div class="row">
          <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
              <div class="card-body">
                
                @if(!servicos.length)
                <div class="alert alert-secondary">
                  <i class="mdi mdi-alert-box icon-md"></i> Nenhum serviço encontrado na base de dados!
                </div>
                @end
                @if(servicos.length)
                <div class="table-responsive">
                  <table class="table">
                    <thead>
                      <tr>
                        <th style="font-weight: bold; color: green;">Nome</th>
                        <th style="font-weight: bold; color: green;">Descrição</th>
                        <th style="font-weight: bold; color: green;">Valor</th>
                        <th style="font-weight: bold; color: green;">Opções</th>
                      </tr>
                    </thead>
                    <tbody>
                      @each((item, index) in servicos)
                      <tr>
                        <td>{{ item.nome }}</td>
                        <td>{{ item.descricao }}</td>
                        <td>{{ item.valor }}</td>
                        <td style="display: flex;">
                          <a href="{{ route('servicos.edit', [item.id]) }}" class="btn-action-green mr-1">
                            <i class="mdi mdi-border-color"></i>
                          </a>
                          <form id="form-delete-{{ item.id }}" method="POST"
                            action="{{
                              route('servicos.destroy', { id: item.id }, { qs: { _method: 'DELETE' } })
                            }}">
                            {{ csrfField() }}
                            <a href="#" onclick="document.getElementById('form-delete-{{item.id}}').submit();" class="btn-action-green mr-1">
                              <i class="mdi mdi-delete-variant"></i>
                            </a>
                          </form>
                        </td>
                      </tr>
                      @end
                    </tbody>
                  </table>
                </div>
                @end
              </div>
            </div>
          </div>
        </div>
      </main>
    </div>
  </div>

  <script src="/resources/vendors/js/vendor.bundle.base.js"></script>
  <script src="/resources/js/off-canvas.js"></script>
  <script src="/resources/js/hoverable-collapse.js"></script>
  <script src="/resources/js/misc.js"></script>
  <script src="/resources/js/settings.js"></script>
  <script src="/resources/js/todolist.js"></script>
</body>

</html>
